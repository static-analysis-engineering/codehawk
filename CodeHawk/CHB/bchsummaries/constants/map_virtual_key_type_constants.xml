<?xml version="1.0" encoding="UTF-8"?>
<codehawk-binary-analyzer>
  <header name="hresult" creation-date="2015-02-07" last-revision="2015-02-07">
    <reference href="http://msdn.microsoft.com/"/>
  </header>
  <symbolic-constants type="map_virtual_key_type_constants">
    <type>map_virtual_key_type_constants</type>
    <values>
    <symc name="MAPVK_VK_TO_CHAR" value="2">
      <desc>
	The uCode parameter is a virtual-key code and is translated into an unshifted 
	character value in the low order word of the return value. Dead keys (diacritics)
	are indicated by setting the top bit of the return value. If there is no translation, 
	the function returns 0.
      </desc>
    </symc>
    <symc name="MAPVK_VK_TO_VSC" value="0">
      <desc>
	The uCode parameter is a virtual-key code and is translated into a scan code. If 
	it is a virtual-key code that does not distinguish between left- and right-hand 
	keys, the left-hand scan code is returned. If there is no translation, the 
	function returns 0.
      </desc>
    </symc>
    <symc name="MAPVK_VK_TO_VSC_EX" value="4">
      <desc>
	The uCode parameter is a virtual-key code and is translated into a scan code. 
	If it is a virtual-key code that does not distinguish between left- and 
	right-hand keys, the left-hand scan code is returned. If the scan code is an 
	extended scan code, the high byte of the uCode value can contain either 0xe0 
	or 0xe1 to specify the extended scan code. If there is no translation, the 
	function returns 0.
      </desc>
    </symc>
    <symc name="MAPVK_VSC_TO_VK" value="1">
      <desc>
	The uCode parameter is a scan code and is translated into a virtual-key code 
	that does not distinguish between left- and right-hand keys. If there is no 
	translation, the function returns 0.
      </desc>
    </symc>
    <symc name="MAPVK_VSC_TO_VK_EX" value="3">
      <desc>
	The uCode parameter is a scan code and is translated into a virtual-key code 
	that distinguishes between left- and right-hand keys. If there is no translation, 
	the function returns 0.
      </desc>
    </symc>
    </values>
  </symbolic-constants>
</codehawk-binary-analyzer>